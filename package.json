{
  "name": "getsocialvideobot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix video-bot --production=false --no-audit --no-fund",
    "build": "npm run build --prefix video-bot",
    "start": "npm run start --prefix video-bot"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@fastify/express": "^4.0.2",
    "fastify-express": "^0.3.3"
  }
}
