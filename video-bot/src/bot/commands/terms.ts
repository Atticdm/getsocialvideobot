import { Context } from 'telegraf';
import { logger } from '../../core/logger';
import { trackUserEvent } from '../../core/analytics';

export async function termsCommand(ctx: Context): Promise<void> {
  try {
    const userId = ctx.from?.id;
    const username = ctx.from?.username;

    logger.info('Terms command received', { userId, username });
    trackUserEvent('command.terms', userId, { username });

    const message = `📜 Условия использования

1. Общие положения
Бот предоставляет услуги по скачиванию, переводу и озвучке видео из различных источников (Facebook, Instagram, YouTube, TikTok, LinkedIn, Sora).

2. Использование сервиса
• Бот предназначен для личного использования
• Запрещается использование бота для нарушения авторских прав
• Пользователь несет ответственность за контент, который он скачивает и обрабатывает
• Бот не хранит загруженные пользователями файлы после обработки

3. Платные функции
• Перевод и озвучка видео являются платными функциями
• Каждый пользователь получает 1 бесплатный кредит при первом использовании
• Дополнительные кредиты можно приобрести через Telegram Stars или другие доступные способы оплаты
• Оплаченные кредиты не подлежат возврату, за исключением случаев технических ошибок
• При технической ошибке во время использования кредита, кредит возвращается автоматически

4. Ограничения ответственности
• Бот предоставляется "как есть" без гарантий
• Мы не несем ответственности за качество перевода или озвучки
• Мы не гарантируем доступность сервиса 24/7
• Максимальный размер обрабатываемого видео ограничен техническими возможностями

5. Конфиденциальность
• Мы не собираем и не храним персональные данные пользователей
• Все обрабатываемые файлы удаляются после завершения операции
• Мы используем только необходимые данные для работы бота (user_id для кредитов)

6. Изменения условий
Мы оставляем за собой право изменять условия использования в любое время. Продолжение использования бота означает согласие с новыми условиями.

7. Контакты
По вопросам использования бота обращайтесь через команду /support

Дата последнего обновления: ${new Date().toLocaleDateString('ru-RU')}`;

    await ctx.reply(message, {
      parse_mode: 'HTML',
    });
  } catch (error) {
    logger.error('Error in terms command', { error, userId: ctx.from?.id });
    trackUserEvent('command.terms.error', ctx.from?.id, {
      error: error instanceof Error ? error.message : String(error),
    });
    await ctx.reply('Sorry, something went wrong. Please try again.');
  }
}

