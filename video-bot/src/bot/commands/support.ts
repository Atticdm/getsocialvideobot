import { Context } from 'telegraf';
import { logger } from '../../core/logger';
import { trackUserEvent } from '../../core/analytics';

export async function supportCommand(ctx: Context): Promise<void> {
  try {
    const userId = ctx.from?.id;
    const username = ctx.from?.username;

    logger.info('Support command received', { userId, username });
    trackUserEvent('command.support', userId, { username });

    const message = `💬 Поддержка

Если у вас возникли вопросы или проблемы с использованием бота, мы готовы помочь!

📋 Часто задаваемые вопросы:

❓ Как скачать видео?
Просто отправьте ссылку на поддерживаемое видео (Facebook, Instagram, YouTube, TikTok, LinkedIn, Sora), и бот автоматически скачает оригинал.

❓ Как перевести видео?
Используйте команду /translate или кнопку "🌐 Перевести видео" в меню. Выберите направление перевода и тип (быстрый или качественный).

❓ Как озвучить видео?
Используйте кнопку "🎙 Озвучить видео" в меню. Выберите язык оригинала и голос для озвучки.

❓ Сколько стоит перевод/озвучка?
• Первое использование — бесплатно (1 кредит)
• Дополнительные кредиты можно приобрести через Telegram Stars
• Пакет из 10 кредитов стоит 500 ⭐ Stars ($5)

❓ Что делать, если перевод не работает?
• Проверьте, что ссылка корректна и видео доступно
• Убедитесь, что у вас есть доступные кредиты (команда /credits)
• Попробуйте позже — сервис может быть временно недоступен

❓ Как проверить баланс кредитов?
Используйте команду /credits для просмотра баланса и статистики использования.

❓ Можно ли вернуть деньги за оплаченные кредиты?
Кредиты возвращаются автоматически только в случае технических ошибок во время использования. Возврат средств за неиспользованные кредиты не предусмотрен.

📞 Связь с поддержкой:
Если ваша проблема не решена, опишите её подробно в ответном сообщении, и мы постараемся помочь.

📚 Полезные команды:
/help — полная справка по командам
/status — информация о состоянии сервиса
/terms — условия использования
/credits — баланс кредитов`;

    await ctx.reply(message);
  } catch (error) {
    logger.error('Error in support command', { error, userId: ctx.from?.id });
    trackUserEvent('command.support.error', ctx.from?.id, {
      error: error instanceof Error ? error.message : String(error),
    });
    await ctx.reply('Sorry, something went wrong. Please try again.');
  }
}

