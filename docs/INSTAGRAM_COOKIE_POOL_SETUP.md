# Настройка пула cookies для Instagram

## Зачем нужен пул cookies?

При использовании бота большим количеством пользователей (10,000+) одна cookie быстро заблокируется Instagram из-за rate limiting. Пул cookies позволяет:

- ✅ **Распределить нагрузку** между множественными аккаунтами
- ✅ **Автоматическую ротацию** cookies для равномерного использования
- ✅ **Автоматическое исключение** заблокированных cookies
- ✅ **Отслеживание статистики** использования каждой cookie

## Формат пула cookies

Пул cookies хранится в переменной окружения `INSTAGRAM_COOKIES_POOL_B64` в формате base64-encoded JSON:

```json
[
  {
    "id": "cookie1",
    "cookiesB64": "base64_encoded_cookies_txt_file_here"
  },
  {
    "id": "cookie2", 
    "cookiesB64": "base64_encoded_cookies_txt_file_here"
  },
  {
    "id": "cookie3",
    "cookiesB64": "base64_encoded_cookies_txt_file_here"
  }
]
```

## Как создать пул cookies

### Шаг 1: Получите cookies для каждого аккаунта

Для каждого аккаунта Instagram:

1. **Установите расширение для экспорта cookies:**
   - Chrome/Edge: [Get cookies.txt LOCALLY](https://chrome.google.com/webstore/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
   - Firefox: [cookies.txt](https://addons.mozilla.org/en-US/firefox/addon/cookies-txt/)

2. **Экспортируйте cookies:**
   - Откройте Instagram в браузере и авторизуйтесь
   - Нажмите на иконку расширения
   - Выберите домен `instagram.com`
   - Сохраните файл как `instagram_cookies_account1.txt`

3. **Повторите для каждого аккаунта:**
   - `instagram_cookies_account1.txt`
   - `instagram_cookies_account2.txt`
   - `instagram_cookies_account3.txt`
   - ... и так далее

### Шаг 2: Конвертируйте каждый файл в Base64

```bash
# macOS/Linux
base64 -w0 instagram_cookies_account1.txt > account1_b64.txt
base64 -w0 instagram_cookies_account2.txt > account2_b64.txt
base64 -w0 instagram_cookies_account3.txt > account3_b64.txt

# Windows (PowerShell)
[Convert]::ToBase64String([IO.File]::ReadAllBytes("instagram_cookies_account1.txt")) | Out-File -Encoding ASCII account1_b64.txt
```

### Шаг 3: Создайте JSON файл пула

Создайте файл `cookie_pool.json`:

```json
[
  {
    "id": "account1",
    "cookiesB64": "PASTE_CONTENT_FROM_account1_b64.txt_HERE"
  },
  {
    "id": "account2",
    "cookiesB64": "PASTE_CONTENT_FROM_account2_b64.txt_HERE"
  },
  {
    "id": "account3",
    "cookiesB64": "PASTE_CONTENT_FROM_account3_b64.txt_HERE"
  }
]
```

**Важно:** Вставляйте содержимое файлов `account1_b64.txt` и т.д. БЕЗ переносов строк и кавычек.

### Шаг 4: Конвертируйте JSON в Base64

```bash
# macOS/Linux
base64 -w0 cookie_pool.json > cookie_pool_b64.txt

# Windows (PowerShell)
[Convert]::ToBase64String([IO.File]::ReadAllBytes("cookie_pool.json")) | Out-File -Encoding ASCII cookie_pool_b64.txt
```

### Шаг 5: Установите переменную окружения

**Railway:**
1. Откройте настройки проекта на Railway
2. Перейдите в "Variables"
3. Добавьте переменную:
   - Name: `INSTAGRAM_COOKIES_POOL_B64`
   - Value: `<содержимое файла cookie_pool_b64.txt>`
4. Deploy изменения

**Локально (.env):**
```env
INSTAGRAM_COOKIES_POOL_B64=<содержимое_файла_cookie_pool_b64.txt>
```

## Как работает ротация

1. **Round-robin ротация:** Cookies используются по очереди для равномерного распределения нагрузки
2. **Автоматическое исключение:** После 3 неудачных попыток cookie помечается как заблокированная
3. **Автоматическое восстановление:** Через 1 час счетчик ошибок сбрасывается
4. **Fallback:** Если все cookies заблокированы, система автоматически сбрасывает счетчики

## Рекомендации

### Количество cookies

- **Малый трафик (< 100 запросов/день):** 1-3 cookies
- **Средний трафик (100-1000 запросов/день):** 5-10 cookies
- **Большой трафик (1000+ запросов/день):** 10-50 cookies
- **Очень большой трафик (10,000+ запросов/день):** 50-100+ cookies

### Обновление cookies

- Cookies имеют срок действия (обычно 30-90 дней)
- Рекомендуется обновлять пул каждые 30 дней
- Можно добавлять новые cookies без остановки бота (просто обновите переменную)

### Безопасность

⚠️ **Важно:**
- Используйте отдельные тестовые аккаунты для бота
- Не используйте свои личные аккаунты
- Регулярно проверяйте статус аккаунтов
- Мониторьте логи на предмет блокировок

## Мониторинг

Статистика пула cookies логируется при каждом скачивании:

```json
{
  "cookiePoolStats": {
    "total": 10,
    "available": 8,
    "blocked": 2,
    "cookies": [
      {"id": "account1", "failureCount": 0, "isBlocked": false},
      {"id": "account2", "failureCount": 3, "isBlocked": true},
      ...
    ]
  }
}
```

## Обратная совместимость

Старый формат `INSTAGRAM_COOKIES_B64` (одна cookie) продолжает работать. Если `INSTAGRAM_COOKIES_POOL_B64` не установлена, система использует `INSTAGRAM_COOKIES_B64`.

## Пример скрипта для создания пула

```bash
#!/bin/bash
# create_cookie_pool.sh

POOL_FILE="cookie_pool.json"
echo "[" > $POOL_FILE

for i in {1..10}; do
  if [ -f "instagram_cookies_account${i}.txt" ]; then
    B64=$(base64 -w0 instagram_cookies_account${i}.txt)
    if [ $i -gt 1 ]; then
      echo "," >> $POOL_FILE
    fi
    echo -n "  {\"id\": \"account${i}\", \"cookiesB64\": \"${B64}\"}" >> $POOL_FILE
  fi
done

echo "" >> $POOL_FILE
echo "]" >> $POOL_FILE

# Конвертируем в base64
base64 -w0 $POOL_FILE > cookie_pool_b64.txt
echo "✅ Cookie pool created: cookie_pool_b64.txt"
echo "📋 Copy content to INSTAGRAM_COOKIES_POOL_B64 variable"
```

## Troubleshooting

### "All Instagram cookies are blocked"

**Проблема:** Все cookies в пуле заблокированы.

**Решение:**
- Проверьте логи на предмет ошибок
- Обновите cookies для заблокированных аккаунтов
- Добавьте новые cookies в пул
- Система автоматически сбросит счетчики через 1 час

### "Failed to parse Instagram cookie pool"

**Проблема:** Неправильный формат JSON.

**Решение:**
- Проверьте валидность JSON файла
- Убедитесь, что base64 кодирование выполнено правильно
- Проверьте, что все кавычки экранированы

### Rate limiting

**Проблема:** Instagram блокирует запросы даже с ротацией.

**Решение:**
- Увеличьте количество cookies в пуле
- Добавьте задержки между запросами
- Используйте прокси для дополнительной защиты

